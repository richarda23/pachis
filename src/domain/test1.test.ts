import { test, expect } from 'vitest';


test("is sum", () => {
    expect(1 + 1).toBe(2);
    expect(5 + 5).toBe(10);
})

test("is sum1", () => {
    expect(1 + 1).toBe(2);
    expect(5 + 5).toBe(10);
})